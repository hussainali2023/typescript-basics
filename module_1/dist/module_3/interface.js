"use strict";
const result = {
    name: 'Rohit',
    age: 12,
    role: 'Student'
};
// type addNumberTypes=(num1:number, num2:number)=> number
const addNumber = (num1, num2) => num1 + num2;
const rollNumbers = [1, 5, 7];
console.log(rollNumbers);